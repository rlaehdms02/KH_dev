import pandas as pd

df = pd.read_csv("data/employees.csv")

#Q3-1 열별로 결측치가 몇 개인지 세세요.
# result = df.isna().sum()
# print(result)

#Q3-2 완전히 똑같은 중복행이 몇 개인지 세고, 제거 후 행 수를 구하세요.


#Q3-3 도시 표기를 통일(앞뒤 공백 제거 + `SEOUL`→`서울`)한 뒤, 도시별 인원을 세세요. (중복 제거 후)

#Q3-4 `나이` 결측은 평균으로, `연봉` 결측은 중앙값으로 채우세요.

#Q3-5 결측을 없앤 뒤 `나이`를 정수형(int)으로 바꾸세요.

#Q3-6 이메일을 모두 소문자로 바꾸고, gmail 사용자 수를 세세요. (중복 제거 기준)

#Q3-7 `orders`의 카테고리 표기를 통일(`food`→`식품`, `clothes`→`의류`)한 뒤, 카테고리별 주문 건수를 세세요. (중복 제거)